#!/usr/bin/env bash

file=$1

filename="${file%%.*}"

echo $filename

if [ ! -f $filename ]; then
  tectonic -X compile $file
fi

evince "${filename}.pdf" &> /dev/null & disown

watchexec -c -w $file -- tectonic -X compile $file
